meta {
  name: Check
  type: http
  seq: 2
}

get {
  url: {{host}}/api/auth/cookie/protected
  body: none
  auth: none
}

script:pre-request {
  const cookie = bru.getVar("cookie");
  
  req.setHeader("Cookie", cookie)
}
